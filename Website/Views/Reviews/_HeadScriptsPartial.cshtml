@* SoundManager must be loaded as early as possible. Otherwise browser security may block Flash <-> JS communication after certain JQuery DOM manipulations by a third-party code. *@
@Scripts.Render("~/bundles/soundmanager2")

<style>
    /* #region ----------- Slider */

    #slider-container {
        -webkit-touch-callout: none;
        -webkit-user-select: none;
        -khtml-user-select: none;
        -moz-user-select: -moz-none;
        -ms-user-select: none;
        user-select: none;
    }

    slider {
        display: block;
        position: relative;
        height: 16px;
        width: 100%;
    }

        slider span.bar {
            height: 100%;
            background: #dddddd;
            left: 0;
            z-index: 0;
        }

        slider span.pointer {
            width: 16px;
            top: 0;
            bottom: 0;
            background-color: #007ebd; /* #006394 appBlue */
            border-radius: 4px;
            border: 1px solid #006da3;
        }

        slider span.bubble {
            font-family: 'Courier New', monospace;
            font-size: 13px;
            top: -16px;
            text-align: center;
            text-shadow: none;
        }

            slider span.bubble.limit {
                top: -1px;
            }

    /* The slider directive uses the new HTML5 input[type=range] tag in modern browsers (IE10+). It is the top-most element, so it reacts to the user actions. But its opacity is set to 0, so it is invisible. */
    input[type=range] {
        cursor: pointer;
    }

        input[type=range]::-ms-tooltip {
            display: none;
        }

    /* #endregion ----------- /Slider */

    /* #region ----------- Review editor/viewer */

    div.spot-mark-container {
        width: 100%;
        height: 16px;
        position: relative;
    }

    a.spot-mark {
        background-color: #006394; /* appBlue link-color */
        position: absolute;
        padding: 0px;
        width: 4px;
        height: 100%;
        z-index: 10;
    }

        a.spot-mark:hover {
            background-color: #007ebd; /* appBlue lighten(link-color, 8%) */
        }

        a.spot-mark.active {
            background-color: #ac1f28; /* appRed */
            top: -16px;
        }

    button.unsaved-on-exit {
        border-color: red;
        border-width: 3px;
    }

    /* #endregion ----------- /Review editor/viewer */

    /* #region ----------- Typeahed dropdown menu for tags */

    /*div.tag-input-placeholder > ul.dropdown-menu {
        background-color: white;
    }

        div.tag-input-placeholder > ul.dropdown-menu > li > a {
            color: #333333;
        }

        div.tag-input-placeholder > ul.dropdown-menu > li.active > a {
            color: #333333;
            background-color: #ebebeb;
        }*/

    /* #endregion ----------- /Typeahed dropdown menu for tags */

</style>

